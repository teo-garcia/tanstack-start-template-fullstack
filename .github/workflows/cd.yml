name: CD (mock)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mock-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Record deployment status
        run: |
          echo "## Deployment intentionally disabled" >> "$GITHUB_STEP_SUMMARY"
          echo "This template validates its production build in CI but does not publish artifacts." >> "$GITHUB_STEP_SUMMARY"
